# Coding Agent

## Prerequisites

Copy the example env file and add your Anthropic API key:

```sh
cp .env.example .env
```

Then edit `.env`:

```
ANTHROPIC_API_KEY=your-api-key-here
```

## Run

```sh
go run .
```

## Build

```sh
go build -o chat
./chat
```
